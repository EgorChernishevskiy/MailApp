server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/mail_app
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update